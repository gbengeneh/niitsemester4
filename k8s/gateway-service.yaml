apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: student-app
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 80
      targetPort: 8085
      nodePort: 30007